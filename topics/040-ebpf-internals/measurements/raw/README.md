# Raw evidence

Each dated source-binding directory retains the exact Git commit and archive
digest, the runtime `xxl` resolution receipt, host result archives, archive
digests, and local validation output. The host archives retain eight fresh
privileged process receipts and one ordinary permission-boundary receipt.

Do not infer performance from these records. They contain no elapsed-time
treatment.

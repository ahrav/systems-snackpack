# 2026-07-31 `xxl` x86-64 result

## Boundary

The `xxl` SSH alias resolved to
`dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com` for this run. The run used
commit `20f09a6ef066df02ba4a180cb04b06a96e4dc633` and binary SHA-256
`eceb3ab07abf030d1f5d0e5673c26e75ed565afe79bb9ca6e4a960c3214f2fcd`.
It ran from 16:08:03 through 16:10:35 UTC on CPU 0.

The host reported Linux `6.12.94-123.180.amzn2023.x86_64`, an Intel Xeon
Platinum 8488C, family 6 model 143 stepping 8, 192 online CPUs, two sockets,
48 cores per socket, SMT2, two NUMA nodes, and 64-byte cache lines. The build
used rustc 1.97.1 with LLVM 22.1.6 and
`-C target-cpu=native -C codegen-units=1 -C lto=off`.

## Observed code generation

The final temporal function contains two `VMOVAPS` stores per 64-byte line and
then a plain `MOV` release publication. The candidate contains two
`VMOVNTDQ` stores per line, followed by `SFENCE`, followed by the publication
store. The STLF functions use the same eight-byte store and loads at offsets
zero and four, respectively.

## Measured elapsed time

Each primary interval uses 12 four-process block contrasts. Each A/A interval
uses four blocks. The complete schedule contains 128 fresh processes.
The table reports the timed kernel-wrapper interval. Setup, scrub, and
verification have separate timers. Startup, teardown, serialization, and other
child overhead remain combined in `unmeasured_process_ns`; all are excluded
from `timed_ns`.

| Comparison (`B/A`) | Geometric mean A | Geometric mean B | Ratio | 95% interval |
|---|---:|---:|---:|---:|
| `VMOVNTDQ` / `VMOVAPS` write | 69.502 ms | 26.120 ms | 0.37581 | [0.37407, 0.37756] |
| write A/A | 69.735 ms | 69.692 ms | 0.99938 | [0.99492, 1.00387] |
| partial / exact STLF | 0.747 s | 2.983 s | 3.99082 | [3.87434, 4.11079] |
| STLF A/A | 0.749 s | 0.739 s | 0.98753 | [0.92324, 1.05629] |

The complete-line non-temporal write took about 0.376 times the temporal
elapsed time, or a reciprocal 2.66 times speedup, for this one-pass 512 MiB
workload. The partial overlap took about 3.99 times the exact-overlap time for
this dependent recurrence. All publication, full-pattern, and oracle checks
passed. All 128 validated records reported zero timed minor and major faults.

The A/A intervals include one; that is a diagnostic, not proof that order and
time effects are absent. The STLF A/A interval is comparatively wide. No RFO,
write-combining, store-buffer, or replay counter was sampled, so those causal
mechanisms remain inferred from the workload and observed instructions.

Raw records and retained-file hashes are under [`raw/20f09a6/xxl`](raw/20f09a6/xxl).

# 2026-07-31 AArch64 result

## Boundary

The run used commit `20f09a6ef066df02ba4a180cb04b06a96e4dc633`
and binary SHA-256
`0a9b3cc369da1b6c700b1d83a2d757cea8168a7da854ffb44abbd347168799d0`.
It ran from 16:08:05 through 16:10:05 UTC on CPU 0 of
`dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`.

The host reported Linux `6.12.94-123.190.amzn2023.aarch64`, 64 online CPUs,
one socket, one thread per core, one NUMA node, 64-byte cache lines, Arm
implementer `0x41`, and part `0xd40`. The part ID is consistent with Neoverse
V1; that name is an inference, not a value reported by Linux. The build used
rustc 1.95.0 with LLVM 22.1.2 and
`-C target-cpu=native -C codegen-units=1 -C lto=off`.

## Observed code generation

The final temporal function contains four `STP` instructions per 64-byte line
and then `STLR` publication. The candidate substitutes four `STNP`
instructions and also ends in `STLR`. The STLF functions use the same `STR`
store and `LDUR` loads at offsets zero and four, respectively.

## Measured elapsed time

Each primary interval uses 12 four-process block contrasts. Each A/A interval
uses four blocks. The complete schedule contains 128 fresh processes.
The table reports the timed kernel-wrapper interval. Setup, scrub, and
verification have separate timers. Startup, teardown, serialization, and other
child overhead remain combined in `unmeasured_process_ns`; all are excluded
from `timed_ns`.

| Comparison (`B/A`) | Geometric mean A | Geometric mean B | Ratio | 95% interval |
|---|---:|---:|---:|---:|
| `STNP` / `STP` write | 9.105 ms | 9.127 ms | 1.00235 | [0.99220, 1.01260] |
| write A/A | 9.101 ms | 9.152 ms | 1.00560 | [0.98007, 1.03180] |
| partial / exact STLF | 1.028 s | 1.968 s | 1.91442 | [1.91210, 1.91675] |
| STLF A/A | 1.030 s | 1.029 s | 0.99870 | [0.99727, 1.00014] |

The `STNP` timing is inconclusive under the declared decision rule. The partial
overlap took about 1.91 times the exact-overlap elapsed time for this dependent
recurrence. All publication, full-pattern, and oracle checks passed. All 128
validated records reported zero timed minor and major faults.

These are elapsed-time and final-instruction observations for one host, binary,
CPU, workload, and run window. No cache-allocation, store-buffer occupancy, or
forwarding-replay counter was sampled. The `STNP` allocation behavior and the
cause of the STLF timing difference remain inferred.

Raw records and retained-file hashes are under [`raw/20f09a6/arm`](raw/20f09a6/arm).

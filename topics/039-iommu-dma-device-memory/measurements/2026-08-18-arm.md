# Topic 39 Arm correctness record

Date: 2026-08-18

## What this record establishes

The literal authorized target ran the exact archived Topic 39 source. A generic
build used the Rust compiler's default target features, meaning the instruction
capabilities the compiler may assume. A native build added the build flag
`-C target-cpu=native`; a build flag is an option passed to the compiler, and
this one permits features reported by this host. Eight fresh processes from
each build mode produced the expected deterministic output.
Package formatting and tests passed. The Rust linter Clippy and Rust
documentation generator rustdoc also passed with every warning treated as an
error. Pre/post source identity and receipt validation passed; a receipt is the
retained, machine-verifiable record from one host run. Linked-machine-code
inspection, which decodes the final executable after linking, also passed.

This `2bb0d3e` rerun supersedes the earlier `3aaece9`, `a56a48e`, `068d082`,
`ef1b55f`, and `f43f0fe` receipts while preserving those archives as historical
evidence. It is itself historical: later review commits changed the probe's
receipt contract and the process receipt schema, so it does not validate the
current branch head.

This record does not contain timing. It does not exercise direct memory access
(DMA), where a device moves bytes without a central processing unit (CPU)
instruction for each byte, or an input-output memory management unit (IOMMU),
which translates and restricts device addresses. It also does not exercise a
device; cache maintenance, meaning software operations that make CPU and device
cached views agree; memory-mapped input/output (MMIO) device registers; Address
Translation Services (ATS), which let a device cache translations; Page Request
Interface (PRI) requests for missing translations; or a software input-output
translation lookaside buffer (SWIOTLB), which copies through a device-reachable
bounce buffer.

## Exact identity

- Secure Shell (SSH) label and resolved hostname:
  `dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`
- In the literal `uname` output below, `SMP` means symmetric multiprocessing,
  `UTC` means Coordinated Universal Time, and `GNU/Linux` names the operating
  system environment.
- `uname -a`: `Linux dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com
  6.12.95-124.187.amzn2023.aarch64 #1 SMP Mon Jul 27 20:40:16 UTC 2026
  aarch64 aarch64 aarch64 GNU/Linux`
- Architecture: 64-bit Arm (AArch64), labeled `aarch64` by Linux
- Available and online central processing units (CPUs): 64
- Page size: 4,096 bytes
- Processor identification: Arm implementer `0x41`, part `0xd40`, variant
  `0x1`, revision `0x1`
- Rust: 1.95.0, using the LLVM compiler infrastructure version 22.1.2
- Cargo: 1.95.0
- C compiler: GNU Compiler Collection (GCC) 11.5.0, target
  `aarch64-amazon-linux`
- Generic Rust target feature: Arm Advanced Single Instruction, Multiple Data
  (SIMD), in which one instruction operates on multiple data lanes; Rust names
  this feature `neon`
- Native build flag: `-C target-cpu=native`
- Native C compiler feature query, raw value:
  `armv8.4-a+crypto+sha3+sm4+sve+rng+i8mm+bf16`. It names Armv8.4-A plus
  cryptographic instruction groups, Secure Hash Algorithm 3 (SHA-3), the SM4
  block cipher, Arm Scalable Vector Extension (SVE), random-number support,
  8-bit matrix multiplication, and bfloat16 (`bf16`), a 16-bit floating-point
  format used in machine learning. The same query reported scalable SVE
  vectors and out-of-line atomic-operation helpers, meaning the compiler may
  call shared helper routines instead of embedding every atomic operation. It
  also reported tuning for the Arm Neoverse N1 microarchitecture
  (`neoverse-n1`), meaning performance choices for that particular internal
  processor design without making its optional instructions mandatory.

The processor-identification fields and compiler query are direct
observations. A product-name interpretation of the Main ID Register (MIDR)
fields would be an inference and is not needed for this result.

## IOMMU-facing observations

The measured kernel configuration enabled Peripheral Component Interconnect
(PCI) ATS, PRI, Process Address Space ID (PASID), Virtual Function input/output
(VFIO), and the VFIO type-1 IOMMU backend (`VFIO_IOMMU_TYPE1`), meaning the
implementation behind VFIO's mapping interface. It also enabled IOMMU Shared
Virtual Addressing (SVA), the Arm System Memory Management Unit (SMMU), SMMU
version 3 (SMMUv3), and SWIOTLB. The guest-visible Linux system filesystem
(`sysfs`), the
kernel-created device and configuration tree, contained four PCI devices, zero
IOMMU class entries, zero IOMMU groups, which are kernel isolation sets of
devices that cannot be separated by the IOMMU, and zero PCI IOMMU-group links,
the filesystem links from devices to those groups.

This is configuration and guest-visible directory evidence only. It does not
prove that an IOMMU is active, absent, or bypassed for any real device.

## Generated code and receipts

Generic and native executables retained direct linked calls to both checked
functions: each call instruction names its target directly. The generic checked
translator used AArch64 subtract-and-set-flags (`subs`),
compare-with-zero-and-branch (`cbz`), comparisons, add-and-set-flags (`adds`),
negate (`neg`), and conditional-branch instructions. The decoded executable
machine instructions (disassembly) prove executable structure, not device
behavior or performance.

Every digest below is a content fingerprint computed with Secure Hash Algorithm
256-bit (SHA-256).

- Source commit: `2bb0d3e55efda225caeaeafbb285382824692b64`
- Source archive SHA-256:
  `e5711fbfada35934afb39e4c3492f62a3066b731c05981d35c8dce0d7e31f614`
- Generic executable SHA-256:
  `940774d02ebe94f78817288dcbada131f291819db91878d3308ea88a3a161372`
- Native executable SHA-256:
  `2c7fbd9e83c95fd8d0cda054c359bfacc644baf41ac043daf15a2e01c96e8d46`
- Expected-output SHA-256:
  `9320bc7b8310ecf3e73d33ef03d94685aad4f3982b052ef42987492afb86c4ad`
- Result archive SHA-256:
  `b72811fa59bc2c9ca0bae3ff70ac241d091140d44302b6818950329750fe2da0`
- Validator: `PASS`, 8 generic and 8 native processes, no timing
- Internal file-and-digest manifest: 76 entries, all verified after retrieval

# Topic 39 xxl correctness record

Date: 2026-08-18

## What this record establishes

This host reported the fully qualified hostname the orchestrator supplied for
Secure Shell (SSH) alias `xxl` and confirmed itself as 64-bit x86 (x86-64),
then ran the exact archived Topic 39 source. Binding the `xxl` alias to that
hostname is a client-side resolution the host cannot verify, and no such
resolution artifact is retained here. A generic build used
the Rust compiler's default target features, meaning the instruction
capabilities the compiler may assume. A native build added the build flag
`-C target-cpu=native`; a build flag is an option passed to the compiler, and
this one permits features reported by this host. Eight fresh processes from
each build mode produced the expected deterministic output.
Package formatting and tests passed. The Rust linter Clippy and Rust
documentation generator rustdoc also passed with every warning treated as an
error. Pre/post source identity and receipt validation passed; a receipt is the
retained, machine-verifiable record from one host run. Linked-machine-code
inspection, which decodes the final executable after linking, also passed.

This final-reviewed rerun supersedes the earlier `3aaece9`, `a56a48e`,
`068d082`, `ef1b55f`, and `f43f0fe` receipts for the current branch source
while preserving those archives as historical evidence.

This record does not contain timing. It does not exercise direct memory access
(DMA), where a device moves bytes without a central processing unit (CPU)
instruction for each byte, or an input-output memory management unit (IOMMU),
which translates and restricts device addresses. It also does not exercise a
device; cache maintenance, meaning software operations that make CPU and device
cached views agree; memory-mapped input/output (MMIO) device registers; Address
Translation Services (ATS), which let a device cache translations; Page Request
Interface (PRI) requests for missing translations; or a software input-output
translation lookaside buffer (SWIOTLB), which copies through a device-reachable
bounce buffer.

## Exact identity

- SSH label: `xxl`
- Runtime-resolved hostname:
  `dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com`
- In the literal `uname` output below, `SMP` means symmetric multiprocessing,
  `PREEMPT_DYNAMIC` names Linux's runtime-selectable preemption mode, `UTC`
  means Coordinated Universal Time, and `GNU/Linux` names the operating system
  environment.
- `uname -a`: `Linux dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com
  6.12.95-124.187.amzn2023.x86_64 #1 SMP PREEMPT_DYNAMIC Mon Jul 27
  20:39:41 UTC 2026 x86_64 x86_64 x86_64 GNU/Linux`
- Architecture: 64-bit x86, labeled `x86_64` by Linux
- Available and online central processing units (CPUs): 192
- Page size: 4,096 bytes
- CPU model: Intel Xeon Platinum 8488C under Kernel-based Virtual Machine (KVM)
- Rust: 1.97.1, using the LLVM compiler infrastructure version 22.1.6
- Cargo: 1.97.1
- C compiler: GNU Compiler Collection (GCC) 11.5.0, target
  `x86_64-amazon-linux`
- Generic Rust target feature names: floating-point and vector register-state
  save and restore instructions `FXSAVE` and `FXRSTOR` (`fxsr`), plus Streaming
  Single Instruction, Multiple Data Extensions (SSE) versions 1 (`sse`) and 2
  (`sse2`)
- Native build flag: `-C target-cpu=native`
- Native C feature query: architecture and tuning for the Intel Sapphire Rapids
  microarchitecture (`sapphirerapids`), the particular internal processor
  design that implements the x86-64 instruction set; Advanced Vector Extensions
  2 (AVX2), AVX-512 Foundation (AVX-512F), and SSE version 4.2 enabled

## IOMMU-facing observations

The measured kernel configuration enabled Peripheral Component Interconnect
(PCI) ATS, PRI, Process Address Space ID (PASID), Virtual Function input/output
(VFIO), and the VFIO type-1 IOMMU backend (`VFIO_IOMMU_TYPE1`), meaning the
implementation behind VFIO's mapping interface. It also enabled IOMMU Shared
Virtual Addressing (SVA), Intel IOMMU, Advanced Micro Devices (AMD) IOMMU, and
SWIOTLB. The
guest-visible Linux system filesystem (`sysfs`), the kernel-created device and
configuration tree, contained 154 PCI devices, zero IOMMU class entries, zero
IOMMU groups, which are kernel isolation sets of devices that cannot be
separated by the IOMMU, and zero PCI IOMMU-group links, the filesystem links
from devices to those groups.

This is configuration and guest-visible directory evidence only. It does not
prove that an IOMMU is active, absent, or bypassed for any real device.

## Generated code and receipts

The generic and native executables were position-independent, which means the
loader may place them at different virtual addresses. Each executable loaded
both checked functions through linker-created x86-64 relocation slots. The
loader wrote its chosen executable load base plus a stored adjustment, called
the relocation addend, into each slot. The result was an absolute target
address. Program-counter-relative code reached the slot; the target stored in
the slot was not relative. The executable then made indirect calls whose
absolute target address came from a register. The generic checked translator
used copy (`mov`), bitwise AND that updates condition flags without storing the
result (`test`), subtract (`sub`), write-a-condition-result (`setcc`),
comparisons, add (`add`), negate (`neg`), and conditional-branch instructions.
The decoded executable machine instructions (disassembly) prove executable
structure, not device behavior or performance.

Every digest below is a content fingerprint computed with Secure Hash Algorithm
256-bit (SHA-256).

- Source commit: `2bb0d3e55efda225caeaeafbb285382824692b64`
- Source archive SHA-256:
  `e5711fbfada35934afb39e4c3492f62a3066b731c05981d35c8dce0d7e31f614`
- Generic executable SHA-256:
  `c0a6c1060ddac3653a7e4e8046ea0aca5f35c8e100d83dfc08b0a6459563ebd3`
- Native executable SHA-256:
  `ef6bbfd52e8192f8df428fb11f007c9b81c0391343e7bd43557e89ce1d39e266`
- Expected-output SHA-256:
  `9320bc7b8310ecf3e73d33ef03d94685aad4f3982b052ef42987492afb86c4ad`
- Result archive SHA-256:
  `0f26bc57bd15995f903c43f32be1561ae54613cb44c4b931caf91bfa2a558271`
- Validator: `PASS`, 8 generic and 8 native processes, no timing
- Internal file-and-digest manifest: 76 entries, all verified after retrieval

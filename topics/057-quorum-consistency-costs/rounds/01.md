# Round 01: publish the evidence before returning

The initial standalone Rust example ran from ephemeral scratch on the required
Arm host and runtime-resolved xxl x86 host before repository edits. Both produced
a read inversion and protected all nine majority pairs after write-back.

The artifact expands those controls to exhaustive finite response sets. Timing
would measure local model execution, not network or storage replication. No
performance comparison or instruction-set conclusion is made.

The source freeze, final committed-source host results, generated-code checks,
and validation ledger are recorded in measurements/README.md after replay.

# Correctness evidence

Initial scratch runs on both required Linux hosts reproduced version 1 followed
by version 0, then protected all nine quorum pairs with read write-back.

Final source identity and both-host replay receipts will be added after the
source commit. No latency or throughput is measured by this deterministic model.

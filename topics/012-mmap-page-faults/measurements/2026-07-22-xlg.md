# 2026-07-22 x86-64 host record

The run used source commit `bdd17c6947fbf66207e4bf7204a786d996bad83b`
and archive SHA-256
`74738647fce356a0db4e6901f668ea5cb956778030bbc8300187057c16d5a83b`.
The executing driver and the driver extracted from that archive both had
SHA-256 `d439df3df286dbce573f5e2b09eb3df6d5cb76079dec527dd01baacbe26e35c6`.

The standing `xlg` alias resolved to
`dev-dsk-ahrav-2c-a9191cb6.us-west-2.amazon.com`; the raw manifest records both
the alias and resolved hostname.

## Host boundary

| Field | Observed value |
| --- | --- |
| Alias and resolved host | `xlg`; `dev-dsk-ahrav-2c-a9191cb6.us-west-2.amazon.com` |
| Kernel | `6.12.94-123.180.amzn2023.x86_64`, `PREEMPT_DYNAMIC` |
| CPU | AMD EPYC 9R14 under KVM |
| Online CPUs and NUMA | 128 CPUs, two NUMA nodes, automatic NUMA balancing enabled |
| Base page and THP | 4,096 bytes; THP enabled policy `madvise`; workload used `MADV_NOHUGEPAGE` |
| Memory and swap at probe | 258,514,032 KiB total; 210,093,728 KiB available; no swap |
| File path | `/local/home/ahrav`, XFS on `/dev/nvme0n1p1` |
| C compiler | GCC 11.5.0 with `-O3 -std=c11 -Wall -Wextra -Werror -fno-omit-frame-pointer -march=native` |
| Rust toolchain | rustc 1.93.1, LLVM 21.1.8; Cargo 1.93.1 |
| Rust build flags | `-C target-cpu=native -C debuginfo=1 -C codegen-units=1` |

The host exposed AVX2 and AVX-512 families among its CPU features. Clang was
not installed. The [raw host record](raw/bdd17c6/xlg/host-env.txt) contains the
complete feature and mount output.

## Process results

Each mode used eight fresh CPU-0-pinned processes. `CLOCK_MONOTONIC_RAW`
covered only one volatile byte touch per runtime base page. The setup interval
started inside the workload process and ended before the touch loop. A Python
monotonic boundary covered the complete pinned workload subprocess.

| Mode | Touch median | Mean ± sample SD | Range | Setup median | Fault delta in every process |
| --- | ---: | ---: | ---: | ---: | --- |
| Anonymous first write | 14.845655 ms | 14.935327 ± 0.500036 ms | 14.414730–15.702389 ms | 0.078520 ms | 8,192 minor; 0 major |
| Anonymous read after `MADV_DONTNEED` | 6.882309 ms | 6.904039 ± 0.074915 ms | 6.847229–7.084959 ms | 17.044769 ms | 8,192 minor; 0 major |
| Resident cached file | 1.349075 ms | 1.364795 ± 0.102674 ms | 1.238650–1.530570 ms | 28.909618 ms | 512 minor; 0 major |
| Verified nonresident file | 4,722.963079 ms | 4,730.475993 ± 17.756817 ms | 4,717.707519–4,770.507826 ms | 21.492369 ms | 0 minor; 8,192 major |

Every file-cold process moved from `0/8192` resident pages to `8192/8192`.
Every file-warm process started and ended `8192/8192` resident. The paired
cold/warm touch ratio had median `3,503.583523×`, sample standard deviation
`260.297056×`, and range `3,084.579260–3,812.951308×`. These values describe the
eight process blocks, not independent pages or a confidence interval.

The anonymous modes have equal fault counts but different operations. First
write allocates writable memory. The refault mode reads discarded private pages
and can map the shared zero page. Their ratio is not a speedup measurement.

## Correctness and generated code

All workspace gates passed in the extracted source tree. The exact example
printed `validated cold-file evidence and eight-block schedule`. The portable
Rust benchmark ran 5,000,000 pairs of derived-metric calculations in
17,868,160 ns; that boundary does not exercise `mmap` or Linux page faults.

The linked C read loop uses `movzx` from `BYTE PTR [rdi]`, advances by the
runtime page stride, and accumulates the byte. The linked write loop stores with
`mov BYTE PTR [...]`, advances by the runtime stride, and accumulates its
deterministic value. These instructions prove the compiler retained the sparse
volatile accesses. They do not identify PTE, residency, or fault state.

Raw process rows, summaries, gates, manifests, source verification, disassembly,
and bundle checksums are under [the xlg evidence directory](raw/bdd17c6/xlg/).

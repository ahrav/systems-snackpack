# Round 2

- Session: 4 follow-up
- Date: 2026-07-14
- Finding: the median paired runtime/constant ratio was `2.414760x` on Linux
  AArch64 and `1.582993x` on Linux x86-64. Each host ran nine benchmark
  processes pinned to CPU 0.
- Boundary: each process retained six warm-up pairs and 21 measured samples per
  path. Each sample timed one non-inlined, 10,000,000-iteration dependency
  chain.
- Artifacts: two Linux measurement records preserve raw benchmark output,
  workspace gate logs, SHA-256 digests, exact 96.1% sign-based confidence
  intervals for the paired-ratio medians, and release codegen.
- Verification: correctness, the six listed workspace commands, pinned process
  runs, and release assembly inspection passed on both Amazon Linux 2023 hosts.
- Scope: the ratio difference does not establish an ISA comparison because the
  hosts use different processors, and the x86-64 host runs under KVM.

# xxl exact-source correctness record

The `xxl` alias resolved during this run to
`dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com`. The exact-source Topic 52 run
passed there at `2026-08-31T16:13:32Z`.

## Source identity

- Commit: `70c4821cb0241b3acd620b2d2126521728739c15`
- Path-limited archive SHA-256:
  `71d3fd5f65e5a0cef794c058afa5067009c731bacf5464160ff0ab7b4759d486`
- Compiler flags: `-O2 -g -std=c11 -Wall -Wextra -Werror
  -fno-omit-frame-pointer`
- Compiler: GCC 11.5.0, target `x86_64-amazon-linux`
- Probe binary SHA-256:
  `add5025f4aa4179ab41eba0904276ebd13101abdd171da8ec4c955b1deb8cef2`
- Rust toolchain recorded for host identity: Rust 1.98.0, LLVM 22.1.8

Source hashes matched before and after execution. Generated assembly and linked
disassembly retained the `openat`, `fsync`, `renameat`, and checksum paths.

## Host and filesystem

- Architecture: x86-64, little-endian
- Processor: Intel Xeon Platinum 8488C under KVM
- Online processors: 192
- Topology: 2 sockets, 48 cores per socket, 2 threads per core
- NUMA nodes: 2
- Kernel: `6.12.95-124.187.amzn2023.x86_64`
- Experiment filesystem: XFS on `/dev/nvme0n1p1`
- Filesystem block size: 4096 bytes
- Mount options: `rw,noatime,seclabel,attr2,inode64,logbufs=8,logbsize=32k,
  sunit=1024,swidth=1024,noquota`

The successful `cp --reflink=always` control establishes reflink support for
the tested file pair. It does not establish whole-filesystem tree copy-on-write
behavior.

## Observations

| Case | Exit | Recovered application state |
|---|---:|---|
| after write | 101 | valid generation 41 at `current`; temp present |
| after file `fsync` | 102 | valid generation 41 at `current`; temp present |
| after `renameat` | 103 | valid generation 42 at `current`; temp absent |
| after directory `fsync` | 104 | valid generation 42 at `current`; temp absent |
| complete A/A run 1 | 0 | valid generation 42; temp absent |
| complete A/A run 2 | 0 | identical verifier output to run 1 |
| one-byte corruption | 3 | invalid checksum detected |
| reflink-clone mutation | 3 | clone invalid; source remained valid |

Before clone mutation, `filefrag` reported both 4096-byte blocks as shared.
After mutating byte 128, the clone's first block used physical block `55344350`
while the source kept physical block `51075261`. The second block remained
shared at physical block `51075262`.

## Validation boundary

Independent controller validation checked 50 receipt files and returned
`pass: true` and `sealed: true`. The uncompressed receipt-manifest SHA-256 is
`90ebb098b93b77ed6c3a45f4de86cfc48e25de579f9a0febb7463c940aa1320b`.
The read-only receipt archive is retained at
`/Users/ahrav/.codex/learning/advanced-systems-evidence/topic-052/2026-08-31-70c4821/xxl-receipt.tar.gz`
with SHA-256
`57a157f8db500d3825259a63ebfefc9820bbf62699fc4e4882f5a036e55b4f0b`.
The [compact locator](raw/2026-08-31-70c4821/xxl-results.external.txt)
records both digests and the seal state.

This receipt predates the launcher binding, so it carries no `runner_sha256`
record and the current validator rejects it on that check. The validation result
above is the one its contemporary validator returned.

The process exits left Linux, XFS, the page cache, controller, and device live.
This record does not test power loss, XFS replay, torn sectors, delayed I/O
errors, controller-cache persistence, Btrfs, OpenZFS, or physical-media writes.
No timing claim is made.

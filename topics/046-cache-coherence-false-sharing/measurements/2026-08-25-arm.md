# Arm host observation, 2026-08-25

## Scope

- SSH target and runtime hostname:
  `dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`.
- Instance: `c7g.16xlarge`; AArch64 CPU implementer `0x41`, part `0xd40`,
  variant `0x1`, revision `1`. The Neoverse V1 manual maps part `0xd40` to that
  core and the fields identify revision r1p1. They do not identify the full
  Graviton3 coherence fabric.
- Kernel: Linux `6.12.100-125.179.amzn2023.aarch64`.
- Capacity: 64 available processors, one thread per core, one socket, one
  non-uniform memory access (NUMA) node.
- Placement: CPUs 0 and 1; core IDs 0 and 1; package ID 60; sibling lists `0`
  and `1`.
- Cache-line observation: every sysfs cache index for CPU 0 reported 64-byte
  coherence lines.
- Toolchain: `rustc 1.95.0`, LLVM `22.1.2`, `cargo 1.95.0`.
- Build: release mode with `RUSTFLAGS='-C target-cpu=native'` from source commit
  `e4fb542f6640566f8f7fbcd220eb1f52c388df04` and archive SHA-256
  `f0b8c64c9cacfb27314ae7e4ac90102c6ec9709a03e4b2ded759209a3eb0385b`.

## What the timing tests

Each process creates and pins two workers before timing. The timer includes the
start barrier, 10,000,000 relaxed atomic increments per worker, the final CPU
checks, and the done barrier. It excludes process startup, worker creation,
joins, and teardown. The primary schedule contains eight four-process
ABBA/BAAB blocks. Four additional blocks compare padded A with padded B.

All 48 fresh processes passed final-count, layout, affinity, source, runner, and
binary checks. The 16 packed processes had a descriptive median timed interval
of 149,900,690 nanoseconds, with a range of 134,151,636 to 160,169,369. The 16
primary padded processes had a median of 50,382,292 nanoseconds, with a range of
50,263,596 to 50,485,053.

## Paired result

- Packed/padded geometric-mean ratio: **2.9770018**.
- Eight complete block replications.
- Descriptive 95% percentile bootstrap over blocks: **[2.8985279, 3.0598272]**.
- Log-contrast standard deviation: `0.0418586`.
- Block-ratio range: `[2.8147958, 3.1452364]`.
- Padded A/A geometric-mean ratio: `1.0003600`; four blocks; descriptive
  interval `[0.9996922, 1.0010283]`; log-contrast standard deviation `0.0008214`.

The interval covers between-block variation for this exact binary, placement,
and run window. It does not cover other processors, placements, builds, or
workloads.

## Generated code and mechanism boundary

The measured binary's stable loop contains unrolled AArch64 `ldadd`
instructions. This proves the emitted atomic read-modify-write lowering.
Neoverse V1 can route cacheable atomics as near or far operations. The code and
timer do not reveal which path executed, so the result does not prove L1-to-L1
line migration.

`perf c2c record -e list` exited 255 with `memory events not supported`. No
sampled cache-to-cache or snoop-hit-modified evidence is available. The measured
claim is limited to a layout-sensitive elapsed-time difference for atomic
increments on this host.

The sealed raw bundle is
[`raw/2026-08-25-e4fb542f/arm-results.tar.gz`](raw/2026-08-25-e4fb542f/arm-results.tar.gz).

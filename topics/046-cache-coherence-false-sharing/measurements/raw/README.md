# Raw Topic 46 evidence

Each dated source-bound directory retains one Git archive, its digest, the
`xxl` resolution, compressed Arm and x86 host bundles, and validation receipts.
The host bundles contain every scheduled process attempt and the disassembly of
the exact measured binary.

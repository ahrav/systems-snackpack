# Retained Arm observation

Status: retained evidence; host run and receipt validation passed.

The measured source is commit
`c6b76b4429272814c7e3ab57a199c9d2c2d8ce66`, archived with Secure Hash
Algorithm 256-bit (SHA-256) digest
`f6e75b525d82964437d23f74494758ccdddd1bc0da31e3b2971cdf4d9cd913e4`.
The retrieved result archive is `raw/2026-08-17-c6b76b4/arm-results.tar.gz`,
SHA-256
`dcea29d8131846a50fd1f3da3a9efa618a0a0068d953bfd4734b9f159a494877`.
The later evidence-only commit that retains this archive and note does not
change the measured experiment source.

## Host and build

- Required target: `dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`
- Architecture: `aarch64`
- Kernel release: `6.12.95-124.187.amzn2023.aarch64`
- Processor evidence: `aarch64`; Main ID Register (MIDR) implementer `0x41`,
  part `0xd40`, variant `0x1`, revision 1; one 64-core socket with one thread per
  core
- Available and allowed CPUs: 64, list `0-63`; sender CPU 0 and receiver CPU 1
- Page size: 4,096 bytes
- Payload filesystem: `tmpfs`
- C compiler: GCC 11.5.0 20240719, target `aarch64-amazon-linux`
- Rust compiler: `rustc 1.95.0 (59807616e 2026-04-14)`, LLVM 22.1.2
- Cargo: `cargo 1.95.0 (f2d3ce0bd 2026-03-21)`
- Python: 3.9.25
- Linker and object dump: GNU binutils 2.41-50.amzn2023.0.5
- Generic C flags: `-O3 -g -std=c11 -Wall -Wextra -Werror
  -fno-omit-frame-pointer`; the native build adds `-march=native`
- Generic Rust release build: empty `RUSTFLAGS`; native Rust release build:
  `RUSTFLAGS=-C target-cpu=native`
- Timed native transfer executable SHA-256:
  `4590898f29d980adce4717cdef03b7686c0b26a391209802a3c1702e52cb9e8f`
- Transfer C source SHA-256:
  `3aa13f0ea4701617cfba297986404e3e44bdec37e6d2294f2ef913237eeafd16`
- Locked-memory limit: 8,192 KiB

## Workload and result

The retained run began at 2026-08-17 14:48:57 UTC. It used a prewarmed 512 MiB
file on memory-backed temporary storage, an
IPv4 loopback TCP socket, and 256 KiB requested chunks. Each of eight paired,
order-balanced blocks contained four fresh processes. There was no retry.

Confidence interval (CI) below means the descriptive Student-t 95-percent
working-model interval. Standard deviation (SD) is the sample SD of the eight
complete-block log contrasts. Ratios, CIs, and SDs are calculated from the
process observations; the medians are the exact values in retained
`summary.tsv`.

| Pair; ratio is B / A | Blocks | Processes | Elapsed ratio | 95% CI | SD of log contrasts | Median A | Median B |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| buffered A; `sendfile` B | 8 | 32 | 0.535180981 | [0.528409683, 0.542039049] | 0.015228149 | 0.185384020 s | 0.099415578 s |
| buffered A; `splice` B | 8 | 32 | 0.538883052 | [0.536157533, 0.541622426] | 0.006064140 | 0.184253430 s | 0.099232702 s |
| buffered A; buffered B | 8 | 32 | 1.000467926 | [0.996529935, 1.004421478] | 0.004716735 | 0.184354762 s | 0.183927217 s |

Central processing unit (CPU) medians are descriptive process summaries, not
paired interval estimates:

| Pair | Sender CPU median A / B | Receiver CPU median A / B |
| --- | ---: | ---: |
| buffered / `sendfile` | 0.185127000 s / 0.052104000 s | 0.051852000 s / 0.099501500 s |
| buffered / `splice` | 0.183933000 s / 0.058575500 s | 0.051612000 s / 0.099257500 s |
| buffered A / buffered B | 0.184125000 s / 0.183665500 s | 0.051530000 s / 0.051556000 s |

The observed pipe capacity was 65,536 bytes. For 512 MiB, the counters recorded
8,192 completed file-to-pipe and 8,192 completed pipe-to-socket `splice`
operations. Buffered recorded 2,048 completed `pread` and 2,048 completed
`send` operations; `sendfile` recorded 2,048. Attempts retried after `EINTR`
are not counted. Exact-byte correctness passed for 16,777,219 bytes through
every method.

Both generic and native `MSG_ZEROCOPY` controls verified 524,288 received
bytes, retained the aligned buffer through completion, and covered identifiers
0 through 7 with inclusive completion ranges `[0,0]` and `[1,7]`. Both
notifications had zero error and reported copied fallback on loopback. This
was a correctness and buffer-lifetime control only; it supplied no elapsed or
CPU ranking.

Linked disassembly for both generic and native transfer executables contained
external call sites for `pread`, `send`, `sendfile`, and two `splice` calls.
Both completion-control executables contained `setsockopt`, `sendmsg`, and
`recvmsg` call sites. The strict receipts contain exactly five transfer calls
and three completion-control calls per generic or native executable. This is
generated-code evidence about the executables, not proof of the executed
branch, kernel path, copy behavior, or cause of an elapsed difference.

All 96 timing processes and all three exact-byte correctness processes passed.
The retained validator reported `VALIDATION=PASS`, including eight complete
blocks per pair, the buffered A/A control, and completion coverage of 8/8. The
validator recomputed the analyses exactly and checked that every completion's
`ee_code` agreed with its copied-fallback field. The source manifests recorded
before and after the run are byte-identical: each contains 1,773 entries and
has SHA-256
`0b2ceed67acaf154b8aaf1bbf75d05f629c1b39d279dc003555cc3690b222688`.
After retrieval, the result-archive digest matched and all 272 entries in its
internal manifest verified.

## Boundary

`transfer_sec` starts after input-file open and sender socket connect. It
includes buffer allocation and release for buffered I/O and pipe creation and
close for `splice`. There is no receiver-ready barrier, so receiver `accept`
and buffer allocation can overlap the beginning. It ends after sender shutdown,
receiver report collection, and child exit. Sender CPU covers this interval;
receiver CPU covers the child's whole lifetime. This timer asymmetry is part of
the declared treatment, so the elapsed ratios cannot isolate byte-copy cost.

Measured evidence consists of process times and CPU intervals, exact byte
counts and completed non-`EINTR` operation counts, pipe and socket sizes,
completion records, host and toolchain
metadata, hashes, and linked call sites. Ratios, medians, dispersion, and CIs
are derived summaries. Copy removal, cache, page-reference, protocol, and
scheduler explanations remain inferred because this run did not isolate them.

The result excludes storage misses, a network interface card (NIC), direct
memory access (DMA), a remote receiver, Transport Layer Security (TLS),
congestion, concurrency, and tail latency. It describes this exact host and one
sequential run only; it does not establish a property of Arm processors.

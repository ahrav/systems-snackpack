# Configured live x86 host: 2026-07-30

## Endpoint boundary

The instructed endpoint
`dev-dsk-ahrav-2c-b89a08b3.us-west-2.amazon.com` returned WSSH 403 at
2026-07-30 15:03:31Z. The configured `xxl` alias resolved to
`dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com` and was used for this
replacement run. This record does not claim that the literal instructed
endpoint ran successfully.

| Item | Recorded value |
|---|---|
| Runtime alias and host | `xxl`; `dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com` |
| Run window | 2026-07-30 14:59:57Z to 15:01:49Z |
| Architecture and CPU evidence | x86-64; GenuineIntel family 6, model 143, stepping 8; Xeon Platinum 8488C; KVM |
| Topology | 192 online CPUs, two sockets, two threads per core; CPU 0 pinned, sibling CPU 96 |
| Cache evidence | 64-byte L1 data-cache line, 4.5 MiB L1d, 192 MiB L2, 210 MiB L3 |
| Kernel | `6.12.94-123.180.amzn2023.x86_64` |
| Toolchain | rustc/Cargo 1.93.1, LLVM 21.1.8; GCC 11.5.0; perf 6.1.176 |
| Native flags | `-C target-cpu=native -C lto=no -C codegen-units=1` |
| Source | commit `635f8687466c12889a33b6ae7900b0535a1480c1` |
| Source archive | SHA-256 `f18c9f970e39ce5f592223e36094a5df528dbe4e95a87a988d065c621844f7eb` |
| Probe binary | SHA-256 `97953ad87ee9e2c1c81b61f40a7225e5cb689ded971fc572430dba7e4ad9ce61` |
| Nodes | 4,194,304 |

The archive-to-commit, executed-tree, source-stability, evidence-manifest, and
applicable workspace gates all passed.

## Workload and result

The workload, seed, process count, schedule seed, useful-load count, and
analysis were identical to the AArch64 run.

| Quantity | Result |
|---|---:|
| One-chain median | 149.148 ns/load |
| Eight-chain median | 18.207 ns/load |
| Paired one/eight geometric mean | 8.1875 |
| 95% log-t interval | [8.0751, 8.3015] |
| Paired log-ratio SD | 0.01654 |
| A/A geometric mean | 1.0023 |
| A/A 95% log-t interval | [0.9932, 1.0116] |

The interval covers the eight paired processes and their time window. It is not
an interval over CPUs, hosts, or workloads.

Median one/eight setup times were 219.1/222.6 ms; warm times were 598.2/595.8
ms. Median whole-process times were 5.835/1.446 s. The whole-process paired
ratio was 4.0350.

## Generated code and counters

Reviewed x86-64 disassembly showed:

- `topic20_walk_one`: the load into `%esi` supplies the next iteration's
  shifted index, forming one loop-carried address dependency.
- `topic20_walk_eight`: eight loads update `%r11`, `%r8`, `%rax`, `%rbx`,
  `%rdi`, `%r10`, `%r9`, and `%rcx`; the XOR reduction occurs after the loop.

The code-shape statement is measured. Latency overlap is an inference from code
shape plus elapsed time. Generic PMU smoke counters were usable but used the
smaller, unpaired smoke workload, so no counter-based mechanism claim follows.

Raw evidence: [`raw/635f868/x86-live`](raw/635f868/x86-live).

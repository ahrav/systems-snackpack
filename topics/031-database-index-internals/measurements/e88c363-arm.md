# Arm exact-source result: e88c363

## Execution identity

- Secure Shell (SSH) target and resolved host:
  `dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`
- Source commit: `e88c3633d6a12b9787c31ec0612bccd810d5533d`
- Shared source-archive Secure Hash Algorithm 256-bit (SHA-256) digest:
  `57244a793084ead6db1b4598736046fed39843e0712a51c214fefc5dd1215b9a`
- Run window: 2026-08-10 14:24:40.987 to 14:25:37.285 Coordinated
  Universal Time (UTC)
- Kernel: Linux 6.12.95-124.187.amzn2023.aarch64
- Architecture: AArch64; 64 online central processing units (CPUs); one
  non-uniform memory access node
- Processor evidence: Linux reported vendor `ARM`, model `1`, stepping `r1p1`.
  It exposed no model-name string in this receipt.
- Toolchain: Rust 1.93.1 with LLVM 21.1.8, Cargo 1.93.1, GNU Compiler
  Collection (GCC) 11.5.0, GNU objdump 2.41
- Generic build: `RUSTFLAGS` unset; one release code-generation unit
- Native build: `RUSTFLAGS=-C target-cpu=native`; one release code-generation
  unit. Rust reported the Scalable Vector Extension (SVE), the `neon` Advanced
  Single Instruction, Multiple Data feature, Large System Extensions (LSE),
  Advanced Encryption Standard instructions, Secure Hash Algorithm 3
  instructions, and other enabled target features. GCC identified `armv8.4-a`
  extensions and `neoverse-n1` tuning.

## Workload and replication

Each build used 1,048,576 rows and 65,536 deterministic, evenly mixed hit and
miss queries. One process validated both layouts, ran one untimed warmup, and
then executed eight timed passes, or 524,288 steady lookups. Twelve alternating
blocks used narrow-covering-covering-narrow order and its reverse. They produced
48 CPU-0-pinned fresh processes per build and 24 observations per treatment.
Both builds therefore retained 96 processes.

Every process produced checksum `1003443739073492248`. The modeled narrow index
was 16 mebibytes plus a 16 mebibyte payload array. The covering index was 24
mebibytes. Rust's entry sizes matched the 16-byte narrow, 16-byte payload, and
24-byte covering model.

## Native-build result

| Treatment | Median steady time | Median setup | Median nonsteady |
|---|---:|---:|---:|
| Narrow index plus payload | 381.856547 ns/lookup | 24.653 ms | 87.828 ms |
| Covering entry | 314.163466 ns/lookup | 22.590 ms | 82.771 ms |

The complete-block narrow/covering ratio had geometric mean `1.220380`, median
`1.218347`, and range `[1.198747, 1.255135]`. The ABBA and BAAB medians were
`1.230576` and `1.210371`. The nominal 96.14% sign interval for the block-ratio
median was `[1.202430, 1.233358]` under independent, exchangeable, continuous
block contrasts. Shared host conditions mean those assumptions are not
established.

Median process time outside the recorded setup, nonsteady, and steady regions
was 7.489 ms for narrow and 7.461 ms for covering. This outside boundary
includes process startup, runtime initialization, reporting, and runner
overhead; it is not part of the steady estimate.

The generic build produced a geometric complete-block ratio of `1.233395`, with
median per-process times of 397.317966 ns/lookup for narrow and 319.743573
ns/lookup for covering.

## Linked-image observation

The native narrow hook used 16-byte index scaling, loaded a row locator, and
then loaded the 16-byte payload through that locator. The covering hook used a
24-byte entry calculation and loaded the payload at offset eight in the matched
entry. Both hooks retained data-dependent binary-search branches and bounds
checks. These instructions were observed in the linked executable whose
SHA-256 was
`7aa2fe6cec52123892ba33f6854743b92c213fa5381ce3dd1aba8293b06166e1`.

## Validation and boundary

The exact-source runner passed formatting; generic and native package tests;
workspace library, example, and documentation tests; the Rust Clippy linter
with warnings denied; benchmark compilation; the Rustdoc documentation
generator with warnings denied; source-manifest comparison; symbol checks; and
the internal evidence-manifest check.

Elapsed time and generated code are observed. A cache-locality explanation is
an inference because no performance-monitoring-unit counters were collected.
This run does not represent a database, cold pages, transaction visibility,
concurrent mutation, logging, a filesystem, or a storage device. It says
nothing about other Arm processors or AArch64 in general.

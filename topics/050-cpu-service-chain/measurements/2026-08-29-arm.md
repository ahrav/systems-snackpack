# Required AArch64 host

## Measurement boundary

The accepted campaign ran from `2026-08-29T17:01:42Z` through
`2026-08-29T17:01:46Z` on
`dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`. It used source commit
`97572e93a6ee98e14bece7501068d5cedd962571` and source-archive SHA-256
`546dd1fa3cd205fd19bc937198281e0b7b6ca929a85d657c18f68d8312c4d035`.
The native binary SHA-256 was
`6707b32cd472bc1ac87e5937490635bd922a1b470843237ff558ec951e63e713`.

The host reported:

- `aarch64`, kernel `6.12.100-125.179.amzn2023.aarch64`;
- 64 configured and 64 affinity-allowed logical CPUs;
- Arm implementer `0x41`, part `0xd40`, and one thread per core;
- holder CPU 0, waiter CPU 1, and control CPU 2 on three distinct physical
  core identifiers;
- simultaneous multithreading (SMT) state `0` and control `notimplemented`;
- GNU Compiler Collection (GCC) 11.5.0 and Python 3.9.25;
- build flags `-O2 -g -std=c11 -Wall -Wextra -Werror -pthread`; and
- no exposed CPU-frequency policy files or CPU-idle state directories for the
  selected CPUs.

Absent exposed interfaces do not prove that frequency or idle-state behavior
is absent in the underlying hardware or virtualization layer.

## Exact-host result

Each label contains 16 fresh processes. A complete four-process block is one
replication, so every interval below uses eight block contrasts.

| Process observation | Same-CPU A | Separate-core B |
|---|---:|---:|
| Median holder thread CPU time | 5.015706 ms | 5.019266 ms |
| Median holder wall time | 192.206376 ms | 5.020124 ms |
| Median waiter lock time | 200.018157 ms | 5.027792 ms |
| Median holder involuntary context switches | 1 | 0 |
| Median complete process-attempt wall time | 201.999408 ms | 7.132101 ms |

The holder-wall A/B geometric ratio was `39.092720`, with a two-sided 95%
Student-t interval of `[38.567194, 39.625406]`. The waiter-lock A/B ratio was
`40.101059`, with interval `[39.716012, 40.489840]`.

The complete attempt time includes fresh-process startup, thread creation, the
measured work, and exit. It is retained separately. The primary holder and
waiter intervals start inside the process after thread placement and the start
barrier, so they exclude compilation and process startup. The experiment does
not estimate startup as its own component.

The mechanically identical X/Y A/A holder-wall ratio was `0.999398`, with
interval `[0.995110, 1.003706]`. Its waiter ratio was `0.995586`, with interval
`[0.986898, 1.004350]`. A/A is a path-asymmetry check, not a noise floor.

All 64 rows used distinct process identifiers. Every holder measured from 4.9
through 6.0 milliseconds of thread CPU time. Affinity, start and end CPUs,
scheduling policy, static priority, nice values, source hashes, and binary
hashes passed independent validation. No attempt failed.

## Linked-code observation

The linked AArch64 `burn_thread_cpu` function remains a distinct symbol. Its
inner loop loads and stores the volatile state, applies shifted `eor`
operations and `mul`, counts 4,096 iterations with `subs`/`b.ne`, calls
`clock_gettime(CLOCK_THREAD_CPUTIME_ID)`, and loops until the CPU-time target.
`holder_main` calls this symbol while the mutex is held. This establishes the
linked code shape; it does not identify a scheduler decision or compare
instruction costs with x86.

## Measured and inferred

Measured: thread CPU and wall time, waiter lock time, context-switch counts,
thread placement and scheduler readback, host metadata, exact source and binary
identity, and linked disassembly.

Inferred: the 39-fold wall expansion with nearly unchanged holder CPU time is
consistent with the nice-19 owner being descheduled by the normal-priority hog.
The involuntary context-switch counts support that explanation but do not make
it unique.

A separate read-only probe at `2026-08-29T17:04:06Z` observed
`/sys/kernel/sched_ext/state=disabled` and
`/proc/sys/kernel/sched_schedstats=0`. It was collected after the campaign, so
it is not rewritten as an in-receipt or whole-window observation.

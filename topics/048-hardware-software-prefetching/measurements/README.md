# Topic 48 measurement records

Final host summaries are added after one checked-source campaign passes on both
required Linux targets. Each note records source commit and archive digest,
host identity, toolchain and flags, workload, process-level run count, interval
scope, code generation, validation outcome, and measured-versus-inferred
boundaries.

Raw receipt archives belong under [`raw/`](raw/).

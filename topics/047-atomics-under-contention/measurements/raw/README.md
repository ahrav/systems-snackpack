# Raw measurement records

Each dated source directory binds two host-result archives to one Git source
commit. `SHA256SUMS` covers the outer archives and source metadata. The archives
contain every process attempt and the inputs required by
`experiment/validate_receipts.py`.

# 2026-07-25 x86-64 host record

This run used source commit
`95bd13b16011d38ee325e49eeef196d03db6611e` from archive SHA-256
`ea398c0ef816f0c43d40162ee6f2c827232791817c3c9a1b083a21c03c85eece`.
The archive hash matched before extraction. The source manifest matched the
archived commit candidate after the run. Later checked-in files are
measurement evidence, not changes to the executed code. All files in the
retained evidence manifest pass `sha256sum -c`. Internal hostnames are
redacted from this record and the retained evidence after capture; the
retained manifest hashes the redacted files.

The former `xlg` host was unreachable during the preflight: the remote-access
proxy refused the connection and could not resolve it. The standing `xlg`
target resolved to a replacement host, and the exact-source run used that
resolved host (`redacted-xlg-host`).

## Host boundary

| Field | Observed value |
| --- | --- |
| Current resolved host | `redacted-xlg-host` (internal identity withheld) |
| Probe time | `2026-07-25T16:50:38Z` |
| `uname` and kernel | Linux x86-64; `6.12.94-123.180.amzn2023.x86_64`, `PREEMPT_DYNAMIC` |
| CPU model evidence | Product `c7a.32xlarge`; AMD EPYC 9R14, family 25, model 17, stepping 1 |
| Online CPUs | 128 |
| Affinity boundary | Runner configured to use `taskset -c 0`; a later probe found `/usr/bin/taskset` 2.37.4, but raw rows do not directly record actual affinity |
| Reported target features | AVX2, AVX-512 families, and the other features in the raw host record |
| Rust toolchain | rustc 1.97.1, LLVM 22.1.6; Cargo 1.97.1 |
| C toolchain | GCC 11.5.0 |
| Focused build | `--release -C target-cpu=native -C codegen-units=1` |
| Linked example SHA-256 | `cd33a277903b506d8b5134f46da00ff49d3d4b4030f978ab70fd712909f514ab` |

## Process results

The run used 12 temporal blocks. Each block launched one fresh `AB` process and
one fresh `BA` process, for 24 processes and 48 timed calls. Launch order
alternated by block. Each process used a 32 MiB target and traversed a 256 MiB
eviction buffer before the pair. Startup, allocation, initialization, and that
eviction traversal were outside the timed intervals.

The IQRs below describe process or block variation in this one run window.
They are not confidence intervals. The 12 blocks are the nominal analysis
units for label comparisons. Independence between block contrasts is an
experimental assumption; the run did not test temporal autocorrelation.

| Metric | Unit count | Median | IQR |
| --- | ---: | ---: | ---: |
| first-position elapsed time | 24 processes | 1.614513 ms | 1.607956–1.623318 ms |
| second-position elapsed time | 24 processes | 1.204901 ms | 1.197216–1.212824 ms |
| first / second | 24 processes | 1.340582x | 1.329829–1.354640x |
| fixed `AB`: label A / B | 12 blocks | 1.333422x | 1.314173–1.356530x |
| fixed `BA`: label A / B | 12 blocks | 0.744930x | 0.740145–0.747487x |
| order-cancelled label A / B | 12 blocks | 0.997165x | 0.982715–1.007531x |

The fixed schedules report large effects in opposite directions even though
labels A and B call the same function. The balanced geometric contrast returns
close to the known A/A identity. Because the A/A truth is known, the opposite
fixed-order estimates falsify using either fixed schedule to attribute a label
effect in this workload and run window.

## Generated code and interpretation

The linked `checksum` body contains a four-accumulator AVX-512 loop using
`vpaddq`, a vector reduction, an AVX2 remainder loop, a scalar tail, and
`vzeroupper`. The linked `measure_pair` body makes both workload calls through
GOT slot `0x598c8`; its relative relocation resolves that slot to the recorded
`checksum` address `0x16330`. These are observations of this native linked
image.

The lower second-position elapsed time is measured. A warmed cache hierarchy
is a plausible explanation because both calls traverse the same 32 MiB target,
but this run did not collect PMU events or cache occupancy and therefore does
not establish that mechanism. It also did not record frequency residency, CPU
isolation, or concurrent host load.

All required Cargo workspace gates passed in the extracted candidate. The
[retained evidence](raw/95bd13b/xlg/) includes raw process rows, summary
statistics, host and toolchain output, source and binary hashes, build flags,
and the linked-code excerpt.

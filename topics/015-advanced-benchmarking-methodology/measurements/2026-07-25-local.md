# Local focused run: 2026-07-25

Host:

- Apple M1 Pro, 10 available CPUs.
- Darwin 25.5.0, arm64.
- rustc 1.93.1, LLVM 21.1.8.
- No CPU affinity. macOS does not provide Linux `taskset`.

Command:

```bash
cargo build --release \
  -p advanced-benchmarking-methodology \
  --example order_bias

topics/015-advanced-benchmarking-methodology/experiment/run_processes.sh \
  target/release/examples/order_bias \
  /tmp/topic15-artifact-local-raw.csv \
  /tmp/topic15-artifact-local-summary.csv \
  12
```

The timer covered each 32 MiB checksum call. Process startup, allocation,
initialization, and the 256 MiB eviction traversal remained outside the timed
regions.

Twenty-four fresh processes formed twelve `AB`/`BA` blocks. Median ratios and
type-7 interquartile ranges were:

| Metric | Median | IQR |
|---|---:|---:|
| First position / second position | 2.503551 | 0.475235 |
| Fixed `AB`, label A / label B | 2.584659 | 0.418526 |
| Fixed `BA`, label A / label B | 0.402976 | 0.084600 |
| Order-cancelled label A / label B | 1.043476 | 0.077683 |

The identical labels reversed apparent winners under fixed order. Balancing
removed most, but not all, of the position effect in this run. The remaining
dispersion rejects a claim that order cancellation guarantees an exact ratio
of `1` at twelve blocks.

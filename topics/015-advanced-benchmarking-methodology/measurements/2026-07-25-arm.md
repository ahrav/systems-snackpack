# 2026-07-25 AArch64 host record

This run used source commit
`95bd13b16011d38ee325e49eeef196d03db6611e` from archive SHA-256
`ea398c0ef816f0c43d40162ee6f2c827232791817c3c9a1b083a21c03c85eece`.
The archive hash matched before extraction. The source manifest matched the
archived commit candidate after the run. Later checked-in files are
measurement evidence, not changes to the executed code. All files in the
retained evidence manifest pass `sha256sum -c`. Internal hostnames are
redacted from this record and the retained evidence after capture; the
retained manifest hashes the redacted files.

## Host boundary

| Field | Observed value |
| --- | --- |
| Requested and resolved host | `redacted-arm-host` (internal identity withheld) |
| Probe time | `2026-07-25T16:50:37Z` |
| `uname` and kernel | Linux AArch64; `6.12.94-123.180.amzn2023.aarch64` |
| CPU model evidence | Product `c7g.16xlarge`; ARM implementer `0x41`, architecture `8`, part `0xd40`, variant `0x1`, revision `1` |
| Online CPUs | 64 |
| Affinity boundary | Runner configured to use `taskset -c 0`; a later probe found `/usr/bin/taskset` 2.37.4, but raw rows do not directly record actual affinity |
| Reported target features | Advanced SIMD, SVE, and the other features in the raw host record |
| Rust toolchain | rustc 1.95.0, LLVM 22.1.2; Cargo 1.95.0 |
| C toolchain | GCC 11.5.0 |
| Focused build | `--release -C target-cpu=native -C codegen-units=1` |
| Linked example SHA-256 | `169a06c259c3b0fd8bc0465f79d62514ab9a172dcf4ac4c1cad3fa80975dc5f6` |

The product name and processor identifiers are observed host evidence. This
record does not infer a commercial CPU name from them.

## Process results

The run used 12 temporal blocks. Each block launched one fresh `AB` process and
one fresh `BA` process, for 24 processes and 48 timed calls. Launch order
alternated by block. Each process used a 32 MiB target and traversed a 256 MiB
eviction buffer before the pair. Startup, allocation, initialization, and that
eviction traversal were outside the timed intervals.

The IQRs below describe process or block variation in this one run window.
They are not confidence intervals. The 12 blocks are the nominal analysis
units for label comparisons. Independence between block contrasts is an
experimental assumption; the run did not test temporal autocorrelation.

| Metric | Unit count | Median | IQR |
| --- | ---: | ---: | ---: |
| first-position elapsed time | 24 processes | 1.126305 ms | 1.120557–1.128230 ms |
| second-position elapsed time | 24 processes | 1.134935 ms | 1.129029–1.140384 ms |
| first / second | 24 processes | 0.992286x | 0.987480–0.996888x |
| fixed `AB`: label A / B | 12 blocks | 0.993444x | 0.989788–0.999396x |
| fixed `BA`: label A / B | 12 blocks | 1.009642x | 1.005644–1.015109x |
| order-cancelled label A / B | 12 blocks | 1.000955x | 0.995425–1.008619x |

The fixed schedules name opposite winners even though labels A and B call the
same function. The balanced geometric contrast returns close to the known A/A
identity. This is a measurement-system result, not evidence that either label
is faster.

## Generated code and interpretation

The linked `checksum` body contains an SVE loop with two vector loads, two
vector accumulators, `uaddv` horizontal reduction, and a scalar tail. The
linked `measure_pair` body calls that same symbol twice. These are observations
of this native linked image.

The run did not collect PMU events, frequency residency, cache occupancy, CPU
isolation, or concurrent-load measurements. Cache state, frequency state, and
interrupt activity are plausible influences, but none is established as the
cause of the position effect.

All required Cargo workspace gates passed in the extracted candidate. The
[retained evidence](raw/95bd13b/arm/) includes raw process rows, summary
statistics, host and toolchain output, source and binary hashes, build flags,
and the linked-code excerpt.

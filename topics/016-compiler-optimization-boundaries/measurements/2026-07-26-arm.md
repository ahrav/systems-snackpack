# Arm Linux record, 2026-07-26

## Boundary

- Target: `redacted-host-arm`
- Source commit: `8b1d2d65f188a0329937789a310dca5b379e3d8f`
- Source verification: collector-verified Git checkout; see the
  [branch receipt](2026-07-26-run-context.md#pushed-source)
- Architecture: `aarch64`
- CPU evidence: implementer `0x41`, part `0xd40`, 64 available CPUs
- Kernel: `6.12.94-123.190.amzn2023.aarch64`
- Toolchain: rustc 1.93.1, LLVM 21.1.8, Cargo 1.93.1
- Native features include NEON and SVE
- Focused flags: `-C target-cpu=native -C codegen-units=1 -C lto=off`
- Affinity: CPU 0
- Run window began: `2026-07-26T13:58:42Z`

The collector reran all six workspace gates on this checkout. All passed.
Local, imported, and opaque modes returned checksum `3170936846` for
1,048,576 words and 32 timed rounds.

## Final linked image

`run_local` and `run_imported` both resolve to `0x158c0`. The shared loop uses
SVE loads and integer operations, followed by `uaddv`, with a scalar remainder.
`run_opaque` resolves to `0x159e0` and its scalar loop executes
`bl topic16_opaque_mix` once per element.

These are observations from the measured executable. They show equal visible
loop bodies and a retained opaque call for this build. They do not make either
source attribute a general optimizer barrier.

## Process experiment

Each comparison used 12 fresh-process pairs in alternating `AB`/`BA` order.
The point estimate is the geometric mean of paired candidate/local ratios. The
fixed-seed 95% bootstrap interval resamples the 12 pair log-ratios, so it covers
process variation in this run window only.

| Comparison | Timed-loop ratio | Bootstrap 95% | Log-ratio sample SD |
| --- | ---: | ---: | ---: |
| imported/local | 1.0064 | [0.9868, 1.0278] | 0.0373 |
| opaque/local | 5.6623 | [5.5558, 5.7664] | 0.0347 |

The whole-process opaque/local ratio was 3.5870
[`3.5654`, `3.6098`]. Whole-process time includes launch, allocation, input
construction, correctness checks, warm-up, and output.

The retained [raw bundle](raw/8b1d2d6/arm) contains the 48 process rows, host
and feature output, exact flags, gate logs, checksums, symbol addresses, source
manifests, the executable digest, and full and focused disassembly. It does not
contain the executable bytes.

# `xxl` exact-source packet-steering record

The accepted Topic 55 campaign ran on runtime-resolved host
`dev-dsk-ahrav-2c-32182091.us-west-2.amazon.com`. The receipt sealed at
`2026-09-04T07:19:55Z`.

## Source and host identity

- Source commit: `d20ee11bbb3c2cef2e98a69194d287783c5e29d6`
- Path-limited archive SHA-256:
  `ceb897467ae5842961acd99bf682f5d5d50346874c09117ebfec890baba8f95e`
- Archived and external runner SHA-256:
  `dfb86492cd0d3c84f2bc4e7584c6b289893301013b9ed839d7dd5181dca3a271`
- Probe source SHA-256:
  `554a5f7b974c85e8c93311a49b90a9668ab264a6ca5d38df0d8b219111ae50ef`
- Native probe binary SHA-256:
  `5a55668b537eb48a1431198bafc3336a8414d42dff446f2bc1732cef411b844d`
- Kernel: `6.12.95-124.187.amzn2023.x86_64`
- CPU topology: 192 x86-64 CPUs, two sockets, two threads per core, two NUMA nodes
- CPU model: Intel Xeon Platinum 8488C under KVM
- Compiler: rustc 1.98.0, LLVM 22.1.8, host `x86_64-unknown-linux-gnu`
- Compile flags: `--edition 2024 -D warnings -C opt-level=3 -C debuginfo=1
  -C target-cpu=generic -C overflow-checks=yes`
- Interface and driver: `eth0`, ENA 2.17.2g
- Queue inventory: 16 RX queues and 16 TX queues

The route to `10.2.101.218` used `eth0` with source `172.16.63.44` before and
after the campaign. It did not use loopback.

## Campaign result

The host ran 24 fresh client processes and 24 fresh server processes. Every
process handled exactly 256 byte-validated observations. Eight client runs
used one live flow with 256 pairs. Sixteen used 128 simultaneously live flows
with two pairs per flow.

Every client flow kept a stable peer, known incoming CPU, and positive stable
NAPI identifier. Every server process observed the expected distinct source
endpoints and complete flow and packet identities. Each 128-flow client run
used 16 incoming CPUs and 16 positive NAPI identifiers. Across the full
campaign the client observed NAPI identifiers `8193` through `8208`.

The observed `eth0` configuration had zero `rps_cpus`, `rps_flow_cnt`,
`xps_cpus`, and `xps_rxqs` values. `net.core.rps_sock_flow_entries` was zero.
These values and IRQ affinity were unchanged after the campaign. `ethtool` was
unavailable, so the receipt does not expose the RSS key, hash fields, or
indirection table.

## Evidence boundary

Observed: the exact host exposed 16 RX queues and 16 distinct positive client
NAPI identifiers while software RPS, RFS, and XPS maps read zero.

Inferred: that result is consistent with receive fanout before optional
software steering. It does not prove an RSS hash, a table entry, or a direct
queue-to-NAPI identity.

Not measured: throughput, latency, interrupt cost, architecture effects, or
the benefit of changing any steering map. Interrupt and softnet snapshots are
diagnostics with ambient traffic, not packet counts.

Independent validation returned `pass: true`, `sealed: true`, and
`measurement_usable: true`. The 149-file manifest SHA-256 is
`68701c1c9c209a4c43f3fb7615239265b3663444031f73fe55125914fb4cbfbb`.
The external receipt location and archive digest are in the
[compact evidence locator](raw/2026-09-04-d20ee11/xxl-results.external.txt).

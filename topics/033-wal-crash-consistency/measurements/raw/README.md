# Raw measurement archives

Each promoted source commit has one sealed archive per required Linux host and
an outer `SHA256SUMS` manifest. Extracted receipts include their own manifest.
The host notes summarize those raw records without replacing them.

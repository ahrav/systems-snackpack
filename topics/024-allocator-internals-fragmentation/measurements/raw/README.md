# Raw evidence

`experiment/run_host.sh` writes evidence outside the source tree. Copy a
validated bundle here under its source identity and host label. Keep the
runner-generated hashes with the bundle.

`c7187b1/` contains the verified AArch64 and runtime-resolved `xxl` bundles
used for the 2026-08-03 comparison.

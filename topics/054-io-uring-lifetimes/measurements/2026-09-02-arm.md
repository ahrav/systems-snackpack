# Arm exact-source correctness record

The exact-source Topic 54 run passed on
`dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com` at
`2026-09-02T16:29:28Z`.

## Source identity

- Commit: `e8f5f459ae012e0b53867224bd6983aa50bd0cd6`
- Path-limited archive SHA-256:
  `867a89efca9d57756fe0fdd57a52a47cd4e89864694e4d654d54ca27c27044c1`
- Runner SHA-256:
  `580cd605e9dd45299dab908f6095f42fa7cba03895f6257113fd75736e0677be`
- Native source SHA-256:
  `ab7e2db7d0c73512f4a94c713976ed9722981323f88026b4d360b4193d4524d9`
- Compiler flags: `-O2 -g -std=c11 -Wall -Wextra -Werror -pthread`
- Compiler: GCC 11.5.0, target `aarch64-amazon-linux`
- Probe binary SHA-256:
  `c2b503b27fa39ad65d9c916f8a697b1b3561761976d7f40b162e2031a0108620`

Source inventories matched before and after execution. The retained assembly
and linked disassembly contain the syscall path used by the probe.

## Host

- Architecture: AArch64, little-endian
- Processor identity: ARM model 1, stepping `r1p1`
- Online processors: 64
- Topology: 1 socket, 64 cores, 1 thread per core
- Kernel: `6.12.100-125.179.amzn2023.aarch64`
- `kernel.io_uring_disabled`: `0`
- Locked-memory limit: 8192 KiB

## Observations

| Check | Observed result |
|---|---|
| baseline setup | SQ 8, CQ 16, features `0xffff` |
| `SINGLE_ISSUER` owner | NOP CQE `user_data=0x1001`, result 0 |
| wrong submitting task | `-EEXIST` (`-17`) |
| CQEs visible before deferred owner entry | 0 |
| deferred terminal CQE | `user_data=0x2001`, `-ETIME` (`-62`) |
| cancel CQE | `user_data=0x3002`, result 0 |
| target terminal CQE | `user_data=0x3001`, `-ECANCELED` (`-125`) |
| process A/A control | normalized outputs identical |

The cancel and target checks compare identities and results without requiring
one completion order.

## Validation boundary

Independent controller validation checked 17 manifest entries and returned
`pass: true` and `sealed: true`. The receipt-manifest SHA-256 is
`a76beed80447a93b8ddb9791d312bdc7ddecee6c947e1eb82333073567c6a063`.
The full read-only receipt is retained at
`/Users/ahrav/.codex/learning/advanced-systems-evidence/topic-054/2026-09-02-e8f5f45/arm/receipt`.
The [compact locator](raw/2026-09-02-e8f5f45/arm-results.external.txt)
binds that directory and manifest.

This is a correctness record. It does not test elapsed performance, storage,
`SQPOLL`, `IOPOLL`, registered resources, multishot operations, provided
buffers, seccomp filters, or Linux Security Module policy.

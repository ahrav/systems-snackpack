# Measurement records

Topic 54 uses a correctness experiment, not a performance benchmark. Each host
record will bind the exact committed source, archive, runner, compiler, kernel,
architecture, policy state, native binary, two process runs, code generation,
and independently validated sealed receipt.

No elapsed-time comparison is collected. Matching observations on two hosts do
not establish an instruction-set or vendor property.

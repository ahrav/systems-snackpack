# dev-dsk-2c Linux run — 2026-07-16

## Host and exact source

- Host: `dev-dsk-ahrav-2c-b89a08b3.us-west-2.amazon.com`
- Architecture: x86-64; 48 online CPUs; measurements pinned to CPU 0
- CPU identity: AMD EPYC 9R14, `AuthenticAMD`, under KVM on Amazon
- Kernel: Linux `6.12.94-123.180.amzn2023.x86_64`
- Compiler: rustc 1.93.1, LLVM 21.1.8; Cargo 1.93.1; GCC 11.5.0
- Native target features include AVX2, AVX-512, BMI1/2, FMA, and VAES
- Build flags: `-C target-cpu=native -C debuginfo=1`
- Source commit: `e3442c23f06ef9c060f869574f532caef46fd04a`
- Archive SHA-256: `e7a8b75eb48aca320f1a7592ea3b37b33692fe53aa115d1a49880e4941a64b2b`

The runner built one benchmark binary, verified 320 queries, then ran 12
blocks. Each block contained one scan process and one index process. Six blocks
used each method order, six used each workload order, and every method-order by
workload-order cell appeared three times.

## Query-phase results

Times are milliseconds. The point estimate is the median of 12 processes. Each
bracket is the third through tenth order statistic. Under independent,
continuous draws, that interval has 96.1426% coverage for the population median.

| Workload | Scan | Index | Paired result |
|---|---:|---:|---:|
| Selective | 1141.027 `[1035.332, 1250.683]` | 6.363 `[6.253, 6.514]` | scan/index `179.600x [158.936, 199.888]` |
| Common/adversarial | 621.545 `[569.321, 674.765]` | 586.365 `[585.532, 586.962]` | index/scan `1.019449 [0.869279, 1.030934]` |

The selective index retained 160 candidates across 160 queries, versus
1,310,720 for the scan. Both methods processed 1,310,720 candidates for the
common/adversarial batch.

The pooled common ratio is not a stable overhead estimate. When common ran
first, the median index/scan ratio was 1.029856: the index was 2.986% slower.
When common ran second, the ratio was 0.869597: the index was 13.040% faster.
The selective speedup also shifted from 159.425x when selective ran second to
199.569x when it ran first. The logs establish an order interaction, but they do
not identify cache, frequency, predictor, allocator, or host activity as its
cause. Four warm-up queries did not establish order-independent steady state.

Index construction took 648.304 ms `[646.756, 649.090]`. Deterministic corpus
and query generation took 13.292 ms `[13.244, 13.322]`, using the mean of the two
process values within each block. These stages and process startup are excluded
from the query timers; process startup itself was not timed.

## Correctness and generated code

The exact oracle passed all 320 queries. Every process returned selective hash
`18349519834868522957` and common/adversarial hash `2924345241696706948`.
The checked source hashes were:

- library: `8afce980abd49bb8857e16d68b593af99f85f6510c98d09b392251152b673353`
- benchmark: `39cc29ba5bdeceb8a5c6878c1286751155afa1ee66f82dbbce1a900cd7b6f776`
- runner: `11d1104612586130fb9ad483f747f1ea2f5e2cb6514571b8bc4d6e32f126f8e8`
- benchmark binary: `2ed2fc37735cd1aee47e87f79693221c663c0f7a3b8d9460c05a7a0802c3d380`

The runner was invoked outside the source tree with relative output `results`.
It wrote both records below the caller directory and did not create
`source/results`. Cargo JSON and an independent `jq` replay selected the same
executable.

The emitted `scan_count` document loop and `contains_exact` first-byte loop use
scalar x86-64 instructions. On a first-byte hit, `contains_exact` calls the
dynamically linked `bcmp`. No explicit XMM, YMM, or ZMM instruction appears in
those two captured functions. This observation does not inspect libc's
runtime-selected `bcmp` implementation. Vector instructions elsewhere in the
compilation unit do not change this focused observation.

## Boundary

The timer spans 160 queries after four warm-up queries. It includes posting
probes and exact verification for the index path, or exact matching over every
document for the scan path. It excludes process startup, data generation, index
construction, warm-up, candidate reporting, and output.

The interval covers sequential process variation in one session, on one CPU,
for one binary, corpus, and query set. The deterministic schedule can alias host
activity, frequency state, or carryover and may violate the interval's
independence assumption. It does not cover other builds, hosts, corpora, query
distributions, AMD CPUs, or x86-64 systems.

See the [final-source raw evidence](raw/final-e3442c2/dev-dsk-2c/).

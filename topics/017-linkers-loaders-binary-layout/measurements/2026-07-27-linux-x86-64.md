# Linux x86-64 binding run: 2026-07-27

## Boundary

- Host: `dev-dsk-ahrav-2c-a9191cb6.us-west-2.amazon.com`
- Run start: `2026-07-27T15:11:06Z`
- Source: `9482befcad700af62e793a7498b7d6c4a71b00d6`
- Archive SHA-256:
  `3ec6a9683a086e6b0e119247a26caceb0cd07ca803259033c3cd1d338110f0e0`
- Kernel: Amazon Linux `6.12.94-123.190.amzn2023.x86_64`
- CPU evidence: `x86_64`, 128 CPUs; `AuthenticAMD`, AMD EPYC 9R14,
  model 17; KVM guest
- Toolchain: GCC 11.5.0, GNU ld 2.41, glibc 2.34. The ELF treatments use
  `cc` and `ld`; no Rust toolchain reaches the measured path. The retained
  `rustc 1.97.1` with LLVM 22.1.6 is the toolchain resolved at the wrapper's
  invocation directory, not the `rust-toolchain.toml` pin of `1.93.1` that the
  archived tree gave the cargo gates.
- Affinity and workload: CPU 0; 4,096 imports; 12 alternating `ABBA` and
  `BAAB` blocks; 48 fresh processes per comparison; 25,000,000 iterations
  in each steady-state child

The interval is a two-sided 95% Student-t interval over 12 process-block
contrasts. It covers block variation in this run window, not future runs or
other machines.

## Results

All values are geometric `B/A` ratios. The causal `B` arm sets
`LD_BIND_NOW=1` on the same `lazy` ELF bytes. The A/A arm runs the lazy
environment under both labels.

| Outcome | Eager/lazy | 95% interval | A/A | A/A 95% interval |
| --- | ---: | ---: | ---: | ---: |
| Parent-observed startup | 1.1333 | [1.1098, 1.1574] | 1.0163 | [0.9929, 1.0403] |
| In-process first use | 0.07967 | [0.07822, 0.08116] | 1.00318 | [0.99877, 1.00760] |
| Resolved steady interval | 0.90577 | [0.89649, 0.91514] | 0.99757 | [0.99226, 1.00290] |

Measured here, eager binding increased the whole-process startup interval and
reduced the first-use interval. This is consistent with moving symbol
resolution before `main`; it does not establish equal total work.

The steady A/A control was narrow around 1, while eager/lazy measured 0.90577.
The resolved call sequence is unchanged by the environment-only treatment,
and this run did not collect evidence that explains the difference. It has no
loader, CPU, or ISA attribution.

## ELF evidence

The fixture check found all 4,096 named `JUMP_SLOT` imports. The lazy final
image uses direct `call ... <probe_0000@plt>` instructions; its PLT entry jumps
through the GOT. The no-PLT image uses `call *... <probe_0000@Base>` through
the GOT. The separately linked `now` image has `BIND_NOW` and `NOW`; the lazy
image does not. Both record a `GNU_RELRO` segment.

The [raw record](raw/9482bef/xlg/) retains host data, build commands, final
ELF hashes, relocations, dynamic tags, program headers, compressed
disassemblies, correctness results, 288 process rows, summaries, repository
gate logs, source manifests, and `evidence.sha256`.

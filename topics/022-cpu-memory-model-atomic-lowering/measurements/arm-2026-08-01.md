# Arm measurement record

## Identity

- Candidate commit: `5f93fdb1472adaf23396ca9c19d0e327adfaab1a`
- Source archive SHA-256: `e024cea840cd416130ab08e2669a909c7046af2f854d8687f5f14a508805b368`
- Capture: `2026-08-01T14:22:58Z`
- Host argument and resolved host: `dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com`
- Architecture and kernel: AArch64, `6.12.94-123.190.amzn2023.aarch64`
- CPU: 64 online CPUs; Arm implementer `0x41`, part `0xd40`, variant `0x1`, revision `1`; MIDR `0x00000000411fd401` captured at `2026-08-01T14:28:41Z`
- Toolchain: rustc 1.95.0, LLVM 22.1.2, Cargo 1.95.0, GCC 11.5.0
- Native Rust features include `lse`, `rcpc`, and `rcpc2`

The source archive matched before extraction. Two library tests, all binary and
example harnesses, and the 100,000-round publication example passed.

## Steady-loop measurements

CPU 3 ran one fresh pinned process at a time. Each ordering comparison used 12
blocks and four processes per block, with alternating ABBA and BAAB order. The
A/A control used two consecutive processes in each of 12 blocks. The estimate
is the median within-block geometric-mean ratio; the IQR and range cover
process blocks. Setup and a separate warmup were recorded but excluded from
elapsed time. The raw median elapsed costs were 0.193659 ns for Relaxed store,
0.193814 ns for Release store, 0.193816 ns for SeqCst store, 5.054001 ns for
Relaxed `fetch_add`, and 5.051502 ns for SeqCst `fetch_add`.

| Ratio | Estimate | IQR | Range |
|---|---:|---:|---:|
| Release / Relaxed store | 1.002173 | 0.999447–1.003217 | 0.997435–1.006507 |
| SeqCst / Release store | 0.999962 | 0.999380–1.003885 | 0.992690–1.005252 |
| SeqCst / Relaxed `fetch_add` | 0.999307 | 0.998539–1.000240 | 0.997284–1.002000 |
| Relaxed store A/A control | 1.000118 | 0.999728–1.003085 | 0.995299–1.007446 |

This measures throughput to one hot private line. It does not measure visibility
latency, cross-core handoff, contention, or startup.

## Generated code and bounded outcomes

Native lowering used `LDR`, `LDAPR`, and `LDAR` for Relaxed, Acquire, and SeqCst
loads; `STR`, `STLR`, and `STLR` for the three store orderings; and
`LDADD`/`LDADDA`/`LDADDL`/`LDADDAL` for the RMW variants. Generic lowering used
`LDAR` for Acquire loads and outline `__aarch64_ldadd8_*` helpers for RMWs.
Acquire fences used `DMB ISHLD`; Release, AcqRel, and SeqCst fences used
`DMB ISH`. The compiler fence emitted no machine instruction.

The final-binary object dumps were inspected but are not checked in. Retained
evidence hashes the native atomic-cost binary as
`d61f9eabc18bb09523bc4ece0a82ddb8a2f22b2bf6ffc2c7c1234c2ac3b20f2a`
and native store-buffering binary as
`31c31520fce1b0b1e1c62e4aa2194f05f22fa186536ce33e17a13812a39eb93c`.

One process ran 1,000,000 store-buffering trials per mode and build. Native
`r00` counts were 762,936 Relaxed, 337,576 Release/Acquire, and 0 SeqCst.
Generic counts were 360,038, 0, and 0. A zero count is finite non-observation,
not a proof that an outcome is forbidden.

[Raw receipts](raw/5f93fdb/arm) retain host metadata, the CPU-identity follow-up,
source and binary hashes, correctness output, every process row, summaries,
litmus counts, and lowering.

# Arm exact-source depth record

The accepted Topic 53 campaign ran on
`dev-dsk-ahrav-2b-7dc7bd93.us-west-2.amazon.com` from
`2026-09-01T18:22:43.074832927Z` through
`2026-09-01T18:26:53.414172382Z`. Only the accepted sealed receipt supplies
the timings in this record.

## Source and build identity

- Source commit: `82c98a25eb4ad31fd9e18fc8d8f9463dab6854d7`
- Path-limited archive prefix:
  `systems-snackpack-82c98a25eb4ad31fd9e18fc8d8f9463dab6854d7/`
- Path-limited archive SHA-256:
  `44085d1001f8566bdf0f7a58af509b8b7acb1f633c09f1f7c218a88ab85e4c45`
- Archived and external launcher SHA-256:
  `b63d48e7ab359611fa2ddf8f354b4caf5f69191d91ed329aa5a034b068b0a376`
- Probe source SHA-256:
  `c0fabb92b6ee4b442dbb293394042058ff441388e577060fb3f23d186fe2f28d`
- Native probe binary SHA-256:
  `6096b67585760969cd66fe94ad8fee336e43e1946cc6ff557e321a37fab5a7d2`
- Compiler: GCC 11.5.0, target `aarch64-amazon-linux`
- Flags: `-O3 -g -fno-omit-frame-pointer -march=native -std=gnu11
  -Wall -Wextra -Werror`

The launcher matched the archived copy. The topic source hashes before and
after execution were identical. Compilation exited zero with empty standard
output and standard error. The binary was a dynamically linked, unstripped
AArch64 ELF executable.

## Host, filesystem, and block path

- Instance identity: `c7g.16xlarge`; AArch64 ARM model 1, stepping `r1p1`
- CPU topology: 64 online CPUs, 1 socket, 64 cores, 1 thread per core, 1 NUMA
  node
- Kernel: `6.12.100-125.179.amzn2023.aarch64`
- Filesystem: XFS root on `/dev/nvme0n1p1`, major:minor `259:1`
- Mount options: `rw,noatime,seclabel,attr2,inode64,logbufs=8,logbsize=32k,
  sunit=1024,swidth=1024,noquota`
- Parent device: 11.7 TiB `nvme0n1`, model `Amazon Elastic Block Store`,
  non-rotational, guest-reported NVMe transport `pcie`, controller state
  `live`, firmware `2.0`
- Stack recorded by the receipt: `nvme0n1p1 -> nvme0n1`; there was no recorded
  device-mapper layer
- Active scheduler: `none`; `nr_requests=63`; logical block 512 bytes;
  physical block 4096 bytes; maximum request 256 KiB; `io_poll=0`;
  `rq_affinity=1`; write-cache report `write through`; writeback-throttling
  target 2000 microseconds
- Exported blk-mq topology: two hardware contexts. Each exposed 63 tags and 0
  reserved tags. Context 0 mapped CPUs 0-31; context 1 mapped CPUs 32-63.
- Controller interrupt-affinity records: IRQ 51 on CPU 46, IRQ 53 on CPUs
  0-31, and IRQ 54 on CPUs 32-63

The session cgroup path was recorded, but its `io.stat`, `io.max`, `io.weight`,
`io.latency`, and `io.cost.*` files were unavailable. No cgroup-limit claim is
made from that absence.

## Fixed campaign

Each timed process ran one userspace thread and exactly 8,192 verified 4 KiB
`O_DIRECT` reads, or 33,554,432 bytes. A private 128 MiB file was initialized,
synced, and verified before the campaign. Linux AIO setup happened before the
timed interval; `io_destroy` happened after it. The timed region covered the
submission/completion loop and data checks.

The depth campaign used eight complete four-process blocks in the fixed order
`ABBA BAAB ABBA BAAB BAAB ABBA BAAB ABBA`. `A` was depth one and `B` was
depth eight. Each letter launched a fresh process. One whole block, not an
individual read or process, was the analysis unit. The fixed horizon was 32
processes and eight block contrasts.

| Application depth | Processes | IOPS minimum | IOPS median | IOPS maximum |
|---:|---:|---:|---:|---:|
| 1 | 16 | 1,739.997 | 1,748.534 | 1,750.474 |
| 8 | 16 | 13,952.510 | 13,974.410 | 14,001.019 |

The predeclared estimate was the geometric mean of the eight whole-block log
IOPS ratios. Depth eight divided by depth one was `7.996451`, with a two-sided
95% Student-t interval of `[7.988423, 8.004487]` and 7 degrees of freedom.
This interval describes between-block variation in this exact host, source,
binary, kernel, filesystem, block stack, and run window. It does not include
host-population, device-population, or rebuild variation.

## A/A control

The separate A/A campaign ran the depth-one artifact through both labels using
`XYYX YXXY XYYX YXXY YXXY XYYX YXXY XYYX`.

| Label | Processes | IOPS minimum | IOPS median | IOPS maximum |
|---|---:|---:|---:|---:|
| X | 16 | 1,744.649 | 1,747.743 | 1,751.471 |
| Y | 16 | 1,743.790 | 1,747.398 | 1,750.101 |

The Y/X point ratio was `0.999347`; its 95% interval was
`[0.998555, 1.000139]`. It passed all predeclared gates: the point was within
`[0.95, 1.05]`, the interval contained 1, and the complete interval remained
inside `[0.90, 1.10]`. This is a mechanical asymmetry check, not proof that all
bias is absent and not a universal noise floor.

## Integrity and code-generation checks

All 64 timed attempts were valid and used 64 unique process identifiers.
Every process reported one thread before and after timing, `pid == tid`, zero
errors, 8,192 verified reads, and 33,554,432 process-accounted read bytes.
Every `STATX_DIOALIGN` query succeeded and reported 4-byte memory alignment and
512-byte offset alignment; the probe used an 8-byte allocation alignment.
Every depth-one process reached `peak_outstanding=1`; every depth-eight process
reached `peak_outstanding=8`. The direct-mode schema's residency fields were
zero sentinels; they do not establish page-cache residency.

The device-wide `nvme0n1` completed-read delta ranged from 8,192 to 8,311
during individual process windows. These counters can include ambient host
traffic and are not additive across the partition and parent device.

Retained AArch64 disassembly shows `submit_all` loading syscall number 2 and
executing `bl <syscall@plt>` for `io_submit`. The direct completion loop loads
syscall number 4 and executes `bl <syscall@plt>` for `io_getevents`. This is
the userspace call boundary into the C library's syscall wrapper. It does not
show the wrapper's eventual trap, blk-mq dispatch, an NVMe command, or a device
completion.

## Evidence boundary

Measured: raising the probe's maximum outstanding Linux AIO reads from one to
eight raised application IOPS by the reported ratio on this exact file path and
run window. The workload integrity, one-thread condition, and A/A gates passed.

Inferred: the result is consistent with greater application-level overlap
keeping more work available to the observed path than depth one did.

Not established: `peak_outstanding=8` does not prove eight requests occupied a
blk-mq hardware context or one NVMe submission queue. A Linux AIO event is not
an NVMe completion-queue entry. The experiment does not isolate filesystem,
blk-mq, driver, hypervisor, EBS service, controller, cache, or media time. The
guest's NVMe name and PCIe transport do not establish local flash.

Independent controller validation returned `pass: true`, `sealed: true`, and
`measurement_usable: true`. The 365-entry receipt manifest SHA-256 is
`eb51120a339f1c1db29c42427f8d01159f1c87eec9ba702e6655d968d67bdc55`.
The external receipt archive and its digest are recorded in the
[compact locator](raw/2026-09-01-82c98a2/arm-results.external.txt).
